spreadsheet:
  url: ""
  sheet_name: Transactions

banks:
  - name: "NTB"
    senders:
      - "^NationsSMS$"
    pattern: "^A TRANSACTION of (?P<currency>[A-Z]{3}) (?P<amount>[0-9,]+\\.[0-9]{2}) was approved on your A/C No\\. (?P<account>[0-9*]+) at (?P<merchant>.+?)\\. Current Bal (?P<balance_currency>[A-Z]{3}) (?P<balance>[0-9,]+\\.[0-9]{2})\\s*\\.?$"

timezone: "UTC"
